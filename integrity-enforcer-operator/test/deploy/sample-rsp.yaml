apiVersion: apis.integrityenforcer.io/v1alpha1
kind: ResourceSigningProfile
metadata:
  name: sample-rsp
spec:
  protectRules:
  - match:
    - kind: ConfigMap
  kustomizePatterns:
  - match:
    - kind: ConfigMap
    namePrefix: "aaaa-"
