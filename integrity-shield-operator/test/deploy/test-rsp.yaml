apiVersion: apis.integrityshield.io/v1alpha1
kind: ResourceSigningProfile
metadata:
  name: test-rsp
spec:
  protectRules:
  - match:
    - kind: ConfigMap
    exclude:
    - name: test-configmap3
  ignoreAttrs:
  - attrs:
    - data.comment
    match:
    - name: test-configmap
      kind: ConfigMap
